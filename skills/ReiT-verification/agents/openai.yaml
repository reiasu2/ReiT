interface:
  display_name: "ReiT Verification"
  short_description: "ReiT pre-completion verification gate"
  default_prompt: "Use $ReiT-verification before claiming this work is complete."

policy:
  allow_implicit_invocation: true
